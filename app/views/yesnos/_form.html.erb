<%= form_for(@yesno) do |f| %>
  <% if @yesno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yesno.errors.count, "error") %> prohibited this yesno from being saved:</h2>

      <ul>
      <% @yesno.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :share %><br>
    <%= f.text_field :share %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
