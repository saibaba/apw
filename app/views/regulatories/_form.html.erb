<%= form_for(@regulatory) do |f| %>
  <% if @regulatory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regulatory.errors.count, "error") %> prohibited this regulatory from being saved:</h2>

      <ul>
      <% @regulatory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :share %><br>
    <%= f.text_field :share %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
